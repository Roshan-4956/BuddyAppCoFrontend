/// Global application configuration settings.
/// Contains constants for controlling application behavior and features.
class GlobalSettings {
  /// Whether to use production server endpoints
  static const bool useProductionServer = true;

  /// Whether to enable device preview mode for development
  static const bool useDevicePreview = false;

  /// Default timeout duration for network requests in seconds
  static const int defaultTimeoutSecs = 7;
}
